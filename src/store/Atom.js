import {atom} from 'recoil';


export const modalusernameState = atom({
    key: 'modalusernameState',
    default: null,
  });

  export const modalpasswordState = atom({
    key: 'modalpasswordState',
    default: null,
  });  
  
  export const modaltState = atom({
    key: 'modaltState',
    default: null,
  });